<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="4" author="medical-system">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="users" columnName="username"/>
            </not>
        </preConditions>
        <addColumn tableName="users">
            <column name="username" type="VARCHAR(50)"/>
        </addColumn>
    </changeSet>

    <changeSet id="5" author="medical-system">
        <sql>
            UPDATE users
            SET username = regexp_replace(split_part(email, '@', 1), '[^a-zA-Z0-9._-]', '', 'g')
            WHERE username IS NULL OR username = '';

            UPDATE users
            SET username = CONCAT('user', id)
            WHERE username IS NULL OR username = '' OR length(username) &lt; 3;
        </sql>
    </changeSet>

</databaseChangeLog>
